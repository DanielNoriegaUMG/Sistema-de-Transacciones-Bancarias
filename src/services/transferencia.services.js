// Fase en desarrollo — conectar a transferencia.model.js cuando la BD esté lista

const getAll = async (user) => {
  return {
    success: true,
    message: "Módulo de transferencias — Fase en desarrollo.",
    data: [],
  };
};

const create = async (body, user) => {
  return {
    success: true,
    message: "Módulo de transferencias — Fase en desarrollo.",
    data: null,
  };
};

module.exports = { getAll, create };
