// Fase en desarrollo — conectar a cuenta.model.js cuando la BD esté lista

const getAll = async (user) => {
  return {
    success: true,
    message: "Módulo de cuentas — Fase en desarrollo.",
    data: [],
  };
};

const getById = async (id, user) => {
  return {
    success: true,
    message: "Módulo de cuentas — Fase en desarrollo.",
    data: null,
  };
};

module.exports = { getAll, getById };
